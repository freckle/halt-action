restylers_version: dev # to get prettier v3
restylers:
  - clang-format:
      enabled: false
  - jq:
      enabled: false # prefer prettier-json
  - prettier:
      include:
        - "src/**/*.ts"
  - whitespace:
      include:
        - "**/*"
        - "!dist/index.js"
  - "*"
